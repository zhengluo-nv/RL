# =============================================================================
# Nemotron 3 Ultra — Reasoning Teacher (non-STEM RLVR, GBS=2048, 65k context)
# =============================================================================
# General reasoning teacher trained against verifiable rewards on a
# non-STEM RLVR data blend (math, MCQA, equivalence-judging, code, NS-tools).
# The output checkpoint serves the `code_gen`, `ns_tools`, `math_with_judge`,
# `equivalence_llm_judge`, and `mcqa` agent slots in MOPD.
#
# Starts from the Student RLVR output. Smaller batch (GBS=2048) and smaller
# expert-model parallelism (EP=32) than the Student RLVR recipe.
#
# Cluster shape (128 nodes × 4 GPUs on GB200 NVL72 = 512 GPUs):
#   Training 64 / vLLM 54 / Gym 10. Override via NUM_TRAIN_NODES, etc.
#   Gym is sized to fit local GenRM (TP=4 DP=4 = 16 GPUs), NL2Bash judge
#   (TP=4 DP=4 = 16 GPUs), and Safety judge (TP=4 DP=2 = 8 GPUs); 40 GPUs
#   total = exactly 10 nodes, no headroom.
#
# Training parallelism: TP=8, EP=32, CP=8, PP=1, SP=true.
# vLLM parallelism: TP=8, EP=8 (EP=TP keeps vllm_dp_size=1; EP>TP is blocked
# by https://github.com/NVIDIA-NeMo/RL/issues/1101).
# =============================================================================

# =============================================================================
# Cluster — read by ultra_launch.sh to size the SLURM allocation
# =============================================================================
cluster:
  gpus_per_node: 4
  num_nodes: 124
  segment_size: 16

# =============================================================================
# Checkpointing — checkpoint_dir is overridden by the launcher
# =============================================================================
checkpointing:
  enabled: true
  checkpoint_dir: "results/ultra_student_rlvr"
  metric_name: "val:total_reward/mean"
  higher_is_better: true
  keep_top_k: 1000000
  save_period: 5
  ft_keep_latest_k: 1
  ft_save_period: 1
  checkpoint_must_save_by: "00:47:35:00"
  model_save_format: "safetensors"
  save_consolidated: false
  save_optimizer: true

# =============================================================================
# GRPO Algorithm
# =============================================================================
grpo:
  num_prompts_per_step: 128
  num_generations_per_prompt: 16
  val_num_generations_per_prompt: 2
  max_rollout_turns: 1
  max_num_epochs: 10
  max_num_steps: 1000000
  normalize_rewards: true
  use_leave_one_out_baseline: true
  advantage_clip_low: -20
  advantage_clip_high: 20
  val_period: 10000
  val_start_at: -1
  val_at_start: false
  val_at_end: false
  overlong_filtering: false
  max_val_samples: null
  # Early stop once this metric (e.g. accuracy or pass_k) reaches the threshold; null disables.
  stop_at_validation_metric: null
  # Required when stop_at_validation_metric is set.
  stop_at_validation_threshold: null
  val_batch_size: 256
  seed: 42

  use_dynamic_sampling: false
  dynamic_sampling_max_gen_batches: 10
  batch_multiplier: 1

  penalize_invalid_tool_call: true
  invalid_tool_call_advantage: -5.0

  reward_shaping:
    enabled: false
    overlong_buffer_length: 128
    overlong_buffer_penalty: 1
    max_response_length: ${policy.max_total_sequence_length}
    stop_properly_penalty_coef: null
  reward_scaling:
    enabled: false
    source_min: 0.0
    source_max: 1.0
    target_min: 0.0
    target_max: 1.0

  async_grpo:
    enabled: true
    max_trajectory_age_steps: 1
    max_generation_failures: 3
    in_flight_weight_updates: true
    recompute_kv_cache_after_weight_updates: false

  use_best_at_k: false
  best_at_k_k: 8
  best_at_k_m: 1000

  use_combined_training: false
  combined_training_weight_mode: "auto"
  combined_training_best_at_k_weight: 0.2
  combined_training_pass_at_1_weight: 1.0

  dynamic_sampling_oversample_ratio: 1.0
  seq_logprob_error_threshold: 2

# =============================================================================
# Loss Function
# =============================================================================
loss_fn:
  reference_policy_kl_penalty: 0.0
  reference_policy_kl_type: "k3"
  kl_input_clamp_value: null
  kl_output_clamp_value: null

  ratio_clip_min: 0.2
  ratio_clip_max: 0.28
  ratio_clip_c: null
  use_on_policy_kl_approximation: true
  use_importance_sampling_correction: true
  truncated_importance_sampling_ratio: 5
  truncated_importance_sampling_ratio_min: 0.2
  truncated_importance_sampling_type: tis
  sequence_level_importance_ratios: false
  token_level_loss: true
  force_on_policy_ratio: true
  use_kl_in_reward: false

# =============================================================================
# Policy — model_name is overridden by the launcher (MODEL_PATH env var)
# =============================================================================
policy:
  model_name: null
  tokenizer:
    name: ${policy.model_name}
    chat_template_kwargs: null
  hf_config_overrides: {}

  train_global_batch_size: 2048
  train_micro_batch_size: 1
  generation_batch_size: 64
  logprob_batch_size: 1
  max_total_sequence_length: 65536
  precision: "bfloat16"
  logprob_chunk_size: 2048
  offload_optimizer_for_logprob: false

  dtensor_cfg:
    _v2: true
    enabled: false
    cpu_offload: false
    sequence_parallel: false
    activation_checkpointing: false
    tensor_parallel_size: 1
    context_parallel_size: 1
    custom_parallel_plan: null

  megatron_cfg:
    enabled: true
    empty_unused_memory_level: 2
    activation_checkpointing: true

    # TP=8 spans 2 GB200 nodes (4 GPUs each) via intra-rack NVLink.
    tensor_model_parallel_size: 8
    expert_tensor_parallel_size: 1
    # EP=32: 512 experts / 32 = 16 experts per EP rank
    expert_model_parallel_size: 32
    pipeline_model_parallel_size: 1
    num_layers_in_first_pipeline_stage: null
    num_layers_in_last_pipeline_stage: null
    context_parallel_size: 8
    pipeline_dtype: ${policy.precision}
    sequence_parallel: true

    # MoE
    freeze_moe_router: true
    moe_router_dtype: "fp32"
    moe_router_load_balancing_type: "none"
    moe_router_bias_update_rate: 1.0e-3
    moe_router_enable_expert_bias: true
    moe_permute_fusion: true
    moe_enable_deepep: false
    moe_token_dispatcher_type: "flex"
    moe_flex_dispatcher_backend: "hybridep"
    moe_hybridep_num_sms: 32
    moe_aux_loss_coeff: 0.0
    moe_shared_expert_overlap: false

    # Compute
    apply_rope_fusion: true
    use_fused_weighted_squared_relu: true
    bias_activation_fusion: false
    gradient_accumulation_fusion: false
    defer_fp32_logits: true

    # Logging
    track_moe_metrics: true
    moe_per_layer_logging: true
    do_not_average_loss: true
    cp_normalize: true
    calculate_per_token_loss: true
    scale_loss_by_dp_cp_size: false

    # MTP — disabled by default; enable via ENABLE_MTP_INFERENCE=1 in the launcher
    mtp_loss_scaling_factor: 0.3
    mtp_use_repeated_layer: true
    mtp_num_layers: 5
    mtp_detach_heads: true

    optimizer:
      optimizer: "adam"
      lr: 3.0e-6
      min_lr: 3.0e-6
      weight_decay: 0.0
      bf16: true
      fp16: false
      params_dtype: "float32"

      adam_beta1: 0.9
      adam_beta2: 0.999
      adam_eps: 1e-8

      sgd_momentum: 0.9

      use_distributed_optimizer: true
      use_precision_aware_optimizer: true

      clip_grad: ${policy.max_grad_norm}

      optimizer_cpu_offload: false
      optimizer_offload_fraction: 0.0

    scheduler:
      start_weight_decay: ${policy.megatron_cfg.optimizer.weight_decay}
      end_weight_decay: ${policy.megatron_cfg.optimizer.weight_decay}
      weight_decay_incr_style: "constant"
      lr_decay_style: "constant"
      lr_decay_iters: null
      lr_warmup_iters: 10
      lr_warmup_init: 3e-7
      override_opt_param_scheduler: true

    distributed_data_parallel_config:
      grad_reduce_in_fp32: false
      overlap_grad_reduce: false
      overlap_param_gather: true
      average_in_collective: false
      use_custom_fsdp: false
      data_parallel_sharding_strategy: "optim_grads_params"

    fp8_cfg:
      enabled: false
      fp8: "e4m3"
      fp8_recipe: "mxfp8"
      fp8_param: false

    first_last_layers_bf16: true
    num_layers_at_start_in_bf16: 1
    num_layers_at_end_in_bf16: 1

    use_gloo_process_groups: false

    checkpoint:
      async_save: true
      ckpt_assume_constant_structure: true
      fully_parallel_save_process_group: "ep_dp"
      fully_parallel_load_process_group: "ep_dp"
      fully_parallel_load_exchange_algo: "broadcast"

    env_vars: null

  # ---------------------------------------------------------------------------
  # Sequence Packing
  # ---------------------------------------------------------------------------
  dynamic_batching:
    enabled: false
    train_mb_tokens: ${mul:${policy.max_total_sequence_length}, ${policy.train_micro_batch_size}}
    logprob_mb_tokens: ${mul:${policy.max_total_sequence_length}, ${policy.logprob_batch_size}}
    sequence_length_round: 64

  sequence_packing:
    enabled: true
    train_mb_tokens: ${mul:${policy.max_total_sequence_length}, ${policy.train_micro_batch_size}}
    logprob_mb_tokens: ${mul:${policy.max_total_sequence_length}, ${policy.logprob_batch_size}}
    algorithm: "modified_first_fit_decreasing"
    sequence_length_round: 64
    fuse_loss: true

  make_sequence_length_divisible_by: ${mul:${policy.megatron_cfg.context_parallel_size}, ${mul:2, ${policy.megatron_cfg.tensor_model_parallel_size}}}
  max_grad_norm: 1.0
  optimizer: null
  scheduler: null

  # ---------------------------------------------------------------------------
  # Generation (vLLM) — Non-colocated, async
  # ---------------------------------------------------------------------------
  generation:
    port_range_low: 3000
    port_range_high: 4999
    backend: "vllm"
    max_new_tokens: ${policy.max_total_sequence_length}
    temperature: 1.0
    top_p: 1.0
    top_k: null
    val_temperature: ${.temperature}
    val_top_p: ${.top_p}
    val_top_k: ${.top_k}
    stop_token_ids: null
    stop_strings: null
    # TP=8 EP=8: EP=TP so vllm_dp_size=1, async_engine=true works with NeMo Gym.
    # Same memory as EP=1 but uses all-to-all for expert routing.
    # EP > TP blocked by https://github.com/NVIDIA-NeMo/RL/issues/1101.
    vllm_cfg:
      async_engine: true
      precision: ${policy.precision}
      kv_cache_dtype: "auto"
      tensor_parallel_size: 8
      pipeline_parallel_size: 1
      expert_parallel_size: 8
      gpu_memory_utilization: 0.85
      max_model_len: ${policy.max_total_sequence_length}
      enforce_eager: false
      use_deep_gemm: false
      num_last_layers_in_bf16: 0
      num_first_layers_in_bf16: 0
      enable_vllm_metrics_logger: true
      vllm_metrics_logger_interval: 0.5
      expose_http_server: true
      skip_tokenizer_init: false
      reasoning_parser_plugin: nemo_rl/models/generation/vllm/reasoning_parsers/nano_v3_reasoning_parser.py
      http_server_serving_chat_kwargs:
        enable_auto_tools: true
        tool_parser: qwen3_coder
        reasoning_parser: nano_v3

    vllm_kwargs:
      moe_backend: flashinfer_cutlass
      attention_backend: FLASH_ATTN
      mamba_ssm_cache_dtype: "float32"
      # Hybrid Mamba: one state slot per running seq; the vLLM default (1024) can
      # exceed available slots. 256 matches the Ultra GB200 serving reference
      # (Gym nemotron_3_ultra_dev_nemorl_gb200.yaml).
      max_num_seqs: 256
      compilation_config:
        cudagraph_capture_sizes: [1,2,4,8,16,32,64]
        cudagraph_mode: PIECEWISE
        pass_config:
          fuse_allreduce_rms: false

    colocated:
      enabled: false
      resources:
        gpus_per_node: 4
        num_nodes: 54   # Overridden by launch script via NUM_GEN_NODES

# =============================================================================
# Data — data_path is overridden by the launcher (TRAIN_PATH / VAL_PATH)
# =============================================================================
data:
  max_input_seq_length: null
  shuffle: false
  num_workers: 1
  use_multiple_dataloader: false
  train:
    data_path: null
  validation:
    data_path: null
  default:
    dataset_name: NemoGymDataset
    env_name: "nemo_gym"
    prompt_file: null
    system_prompt_file: null
    processor: "nemo_gym_data_processor"

# =============================================================================
# Environment — NeMo Gym + NL2Bash judge (only judge needed for Reasoning data)
#
# Judge path is set by the launcher:
#   - nl2bash_judge_model.model ← NL2BASH_JUDGE_MODEL
# =============================================================================
env:
  should_use_nemo_gym: true
  should_log_nemo_gym_responses: true
  nemo_gym:
    nemo_gym_log_dir: "logs/nemo_gym"
    skip_venv_if_present: true
    num_gpu_nodes: 10
    # NeMo-RL-side effort shaping; stripped before forwarding to Gym.
    effort_levels:
      low_string: "{reasoning effort: efficient}"
      low_weight: 0.1
      low_penalty: 1
      low_ub: 15000
    port_range_low: 5000
    port_range_high: 5999
    invalid_tool_call_patterns:
      - "<tool_call>"
      - "</tool_call>"
      - "<function_call>"
      - "</function_call>"
    thinking_tags:
      - "<think>"
      - "</think>"
    # Reasoning data only exercises equivalence_llm_judge_simple_agent.
    # Production loads the full ~35-entry list; we trim to keep gym startup
    # focused.
    config_paths:
      - responses_api_models/vllm_model/configs/vllm_model_for_training.yaml
      - resources_servers/equivalence_llm_judge/configs/equivalence_llm_judge.yaml

    policy_model:
      responses_api_models:
        vllm_model:
          num_workers: 16
          num_groups_nemo_rl: ${add:${grpo.async_grpo.max_trajectory_age_steps}, 1}
          # Model-specific (Nemotron chat template): keep reasoning in multi-turn
          # history so the trajectory grows monotonically for token-in-token-out RL.
          chat_template_kwargs:
            enable_thinking: true
            truncate_history_thinking: false
          sequential_reasoning_allowed: false
    policy_model_reasoning_off:
      _copy: policy_model
      responses_api_models:
        vllm_model:
          num_workers: 4
          num_groups_nemo_rl: ${add:${grpo.async_grpo.max_trajectory_age_steps}, 1}
          chat_template_kwargs:
            enable_thinking: false

    # NL2Bash judge: TP=4 on GB200 192GB
    nl2bash_judge_model:
      responses_api_models:
        local_vllm_model:
          entrypoint: app.py
          model: null   # Set by launcher (NL2BASH_JUDGE_MODEL)
          return_token_id_information: false
          uses_reasoning_parser: false
          debug: true
          ray_worker_py_executable: /opt/ray_venvs/nemo_rl.models.generation.vllm.vllm_worker_async.VllmAsyncGenerationWorker/bin/python
          vllm_serve_env_vars:
            VLLM_RAY_DP_PACK_STRATEGY: strict
            NCCL_MNNVL_ENABLE: "1"
            VLLM_USE_FLASHINFER_MOE_FP16: "0"
            VLLM_USE_FLASHINFER_MOE_FP8: "0"
            VLLM_USE_DEEP_GEMM: "0"
            VLLM_MOE_USE_DEEP_GEMM: "0"

          vllm_serve_kwargs:
            attention_backend: TRITON_ATTN
            tensor_parallel_size: 4
            data_parallel_size: 4
            data_parallel_size_local: 1
            pipeline_parallel_size: 1
            enable_expert_parallel: true
            enable_auto_tool_choice: true
            tool_call_parser: hermes
            gpu_memory_utilization: 0.85
            max_model_len: 131072
            max_num_seqs: 256
            enable_prefix_caching: true
            enable_chunked_prefill: true
            model_loader_extra_config:
              enable_multithread_load: true
              num_threads: 112
            compilation_config:
              cudagraph_capture_sizes: [1,2,4,8,16,32]

    equivalence_llm_judge:
      resources_servers:
        equivalence_llm_judge:
          judge_model_server:
            name: nl2bash_judge_model
          judge_responses_create_params:
            max_output_tokens: 8192


# =============================================================================
# Logger — log_dir, wandb.name, wandb.project are overridden by the launcher
# =============================================================================
logger:
  log_dir: "logs/ultra_student_rlvr"
  num_val_samples_to_print: 0
  wandb_enabled: false
  tensorboard_enabled: false
  mlflow_enabled: false
  monitor_gpus: true
  swanlab_enabled: false
  wandb:
    project: "nemotron-3-ultra"
    name: "ultra-student-rlvr"
  tensorboard: {}
  mlflow:
    experiment_name: "nemotron-3-ultra"
    run_name: "ultra-student-rlvr"
  gpu_monitoring:
    collection_interval: 10
    flush_interval: 10

# =============================================================================
# Reward Penalties (set reward to 0 when triggered)
# =============================================================================
reward_penalties:
  penalize_duplicated_reasoning: true
  penalize_empty_final_answer: true
  penalize_unwanted_tokens: true
  penalize_malformed_think_tag: true
  token_ids:
    unwanted: [2]
    think_open: 12
    think_close: 13
