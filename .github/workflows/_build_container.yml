# Copyright (c) 2026, NVIDIA CORPORATION.  All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
name: Build container

on:
  workflow_call:
    inputs:
      build-ref:
        required: false
        default: ${{ github.sha }}
        description: Ref, branch, or SHA to build.
        type: string
      megatron-lm-commit:
        required: false
        default: ""
        description: Full Megatron-LM commit SHA to check out in the Megatron-Bridge submodule.
        type: string
      megatron-lock-artifact:
        required: false
        default: ""
        description: Run-scoped artifact containing the lockfile for the requested Megatron-LM commit.
        type: string
      image-name:
        required: true
        description: Name of the image to build and push.
        type: string
      build-args:
        required: false
        default: ""
        description: Additional Docker build args.
        type: string
      build-contexts:
        required: false
        default: ""
        description: Additional Docker build contexts.
        type: string
      trtllm-ccache-tag:
        required: false
        default: ""
        description: Prefix for the TRT-LLM ccache image tag; the target architecture and a hash of the effective base image are appended automatically. Empty disables the ccache.
        type: string
      trtllm-wheel-cache-tag:
        required: false
        default: ""
        description: Prefix for the TRT-LLM wheel cache image tag; the target architecture and a hash of the effective base image are appended automatically. Empty disables the cache.
        type: string
      dockerfile:
        required: true
        description: Path to the Dockerfile.
        type: string
      platform:
        required: true
        description: Docker build platform.
        type: string
      runner:
        required: true
        description: Runner to use for the build.
        type: string
      registry:
        required: true
        description: Container registry to push to.
        type: string
      target:
        required: false
        default: ""
        description: Dockerfile stage to build.
        type: string

permissions:
  contents: read
  pull-requests: read

defaults:
  run:
    shell: bash -x -e -u -o pipefail {0}

jobs:
  build:
    runs-on: ${{ inputs.runner }}
    env:
      REGISTRY: ${{ inputs.registry }}
      IMAGE_NAME: ${{ inputs.image-name }}
      GH_REF: ${{ github.ref }}
      RUN_ID: ${{ github.run_id }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ inputs.build-ref }}
          submodules: recursive

      - name: Override Megatron-LM commit
        if: inputs.megatron-lm-commit != ''
        env:
          MEGATRON_LM_COMMIT: ${{ inputs.megatron-lm-commit }}
        run: |
          megatron_lm_dir="3rdparty/Megatron-Bridge-workspace/Megatron-Bridge/3rdparty/Megatron-LM"
          bash .github/scripts/checkout-megatron-lm-commit.sh "$megatron_lm_dir" "$MEGATRON_LM_COMMIT"

      - name: Download Megatron lockfile
        if: inputs.megatron-lock-artifact != ''
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: ${{ inputs.megatron-lock-artifact }}
          path: ${{ runner.temp }}/megatron-lock-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Install Megatron lockfile
        if: inputs.megatron-lock-artifact != ''
        env:
          BUILD_REF: ${{ inputs.build-ref }}
          MEGATRON_LM_COMMIT: ${{ inputs.megatron-lm-commit }}
        run: |
          bash .github/scripts/install-megatron-lock-artifact.sh \
            "$RUNNER_TEMP/megatron-lock-$GITHUB_RUN_ID-$GITHUB_RUN_ATTEMPT" \
            "." "$BUILD_REF" "$MEGATRON_LM_COMMIT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

      - name: Resolve TRT-LLM cache configuration
        id: trtllm_cache
        shell: bash
        env:
          BUILD_ARGS: ${{ inputs.build-args }}
          CCACHE_TAG_PREFIX: ${{ inputs.trtllm-ccache-tag }}
          WHEEL_TAG_PREFIX: ${{ inputs.trtllm-wheel-cache-tag }}
          DOCKERFILE: ${{ inputs.dockerfile }}
          PLATFORM: ${{ inputs.platform }}
        run: |
          set -euo pipefail

          # Match Dockerfile semantics: any non-empty value skips TRT-LLM.
          SKIP_VALUE=$(printf '%s\n' "$BUILD_ARGS" | sed -nE 's/^[[:space:]]*SKIP_TRTLLM_BUILD[[:space:]]*=(.*)$/\1/p' | tail -1)
          SKIP_VALUE=$(printf '%s' "$SKIP_VALUE" | sed -E 's/^[[:space:]]+//; s/[[:space:]]+$//')
          if [[ -n "$SKIP_VALUE" ]]; then
            echo "TRT-LLM build is disabled by SKIP_TRTLLM_BUILD"
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "enabled=true" >> "$GITHUB_OUTPUT"

          # No cache configuration is needed when both image prefixes are empty.
          if [[ -z "$CCACHE_TAG_PREFIX" && -z "$WHEEL_TAG_PREFIX" ]]; then
            exit 0
          fi

          BASE_IMAGE=$(printf '%s\n' "$BUILD_ARGS" | sed -nE 's/^[[:space:]]*BASE_IMAGE[[:space:]]*=[[:space:]]*(.+)[[:space:]]*$/\1/p' | tail -1)
          if [[ -z "$BASE_IMAGE" ]]; then
            BASE_IMAGE=$(sed -nE 's/^[[:space:]]*ARG[[:space:]]+BASE_IMAGE[[:space:]]*=[[:space:]]*(.+)[[:space:]]*$/\1/p' "$DOCKERFILE" | tail -1)
          fi
          if [[ -z "$BASE_IMAGE" ]]; then
            echo "Could not resolve BASE_IMAGE from build args or $DOCKERFILE" >&2
            exit 1
          fi

          ARCH="${PLATFORM#*/}"
          ARCH="${ARCH%%/*}"
          if [[ -z "$ARCH" || "$PLATFORM" != */* || "$PLATFORM" == *,* ]]; then
            echo "Expected a single OS/architecture platform, got: $PLATFORM" >&2
            exit 1
          fi

          CACHE_KEY=$(printf '%s' "$BASE_IMAGE" | sha256sum | cut -c1-12)
          {
            echo "arch=$ARCH"
            if [[ -n "$CCACHE_TAG_PREFIX" ]]; then
              echo "ccache_tag=${CCACHE_TAG_PREFIX}-${ARCH}-${CACHE_KEY}"
            fi
            if [[ -n "$WHEEL_TAG_PREFIX" ]]; then
              echo "wheel_tag=${WHEEL_TAG_PREFIX}-${ARCH}-${CACHE_KEY}"
            fi
          } >> "$GITHUB_OUTPUT"
          echo "TRT-LLM caches use $BASE_IMAGE on $ARCH (key $CACHE_KEY)"

      - name: Resolve TRT-LLM cache seeds
        id: trtllm_cache_seed
        if: steps.trtllm_cache.outputs.ccache_tag != '' || steps.trtllm_cache.outputs.wheel_tag != ''
        shell: bash
        env:
          CCACHE_TAG: ${{ steps.trtllm_cache.outputs.ccache_tag }}
          WHEEL_TAG: ${{ steps.trtllm_cache.outputs.wheel_tag }}
        run: |
          set -euo pipefail

          resolve_seed() {
            local name="$1"
            local tag="$2"
            local context="$3"
            local description="$4"
            [[ -n "$tag" ]] || return 0

            local ref="$REGISTRY/$IMAGE_NAME:$tag"
            if docker buildx imagetools inspect "$ref" >/dev/null 2>&1; then
              echo "Using TRT-LLM $description seed $ref"
              {
                echo "${name}_ref=$ref"
                echo "${name}_context=${context}=docker-image://$ref"
              } >> "$GITHUB_OUTPUT"
            else
              echo "No TRT-LLM $description image found at $ref; starting with an empty cache"
            fi
          }

          resolve_seed ccache "$CCACHE_TAG" trtllm-ccache-seed ccache
          resolve_seed wheel "$WHEEL_TAG" trtllm-wheel-cache-seed "wheel cache"

      - name: Configure TRT-LLM cache exports
        id: trtllm_cache_export
        if: steps.trtllm_cache.outputs.ccache_tag != '' || steps.trtllm_cache.outputs.wheel_tag != ''
        shell: bash
        env:
          CCACHE_SEED_REF: ${{ steps.trtllm_cache_seed.outputs.ccache_ref }}
          CCACHE_TAG: ${{ steps.trtllm_cache.outputs.ccache_tag }}
          WHEEL_SEED_REF: ${{ steps.trtllm_cache_seed.outputs.wheel_ref }}
          WHEEL_TAG: ${{ steps.trtllm_cache.outputs.wheel_tag }}
          PLATFORM: ${{ inputs.platform }}
        run: |
          set -euo pipefail

          configure_export() {
            local name="$1"
            local seed_ref="$2"
            local output_context="$3"
            local description="$4"
            local max_incremental_layers=16
            local cache_layout=incremental-v1
            local export_mode=full

            if [[ -n "$seed_ref" ]]; then
              local image_metadata
              image_metadata=$(docker buildx imagetools inspect "$seed_ref" --format "{{len (index .Image \"$PLATFORM\").RootFS.DiffIDs}} {{index (index .Image \"$PLATFORM\").Config.Labels \"com.nvidia.nemo-rl.cache-layout\"}}" 2>/dev/null || true)
              if [[ -z "$image_metadata" ]]; then
                image_metadata=$(docker buildx imagetools inspect "$seed_ref" --format '{{len .Image.RootFS.DiffIDs}} {{index .Image.Config.Labels "com.nvidia.nemo-rl.cache-layout"}}' 2>/dev/null || true)
              fi

              local layer_count existing_layout
              read -r layer_count existing_layout <<< "$image_metadata"
              if [[ "$existing_layout" == "$cache_layout" && "$layer_count" =~ ^[0-9]+$ ]] && (( layer_count < max_incremental_layers )); then
                export_mode=incremental
                echo "Appending an incremental $description layer to $seed_ref ($layer_count existing layers)"
                echo "${name}_output_context=${output_context}=docker-image://$seed_ref" >> "$GITHUB_OUTPUT"
              else
                echo "Compacting TRT-LLM $description into a full snapshot (layout: ${existing_layout:-legacy}, layers: ${layer_count:-unknown})"
              fi
            fi
            echo "${name}_mode=$export_mode" >> "$GITHUB_OUTPUT"
          }

          if [[ -n "$CCACHE_TAG" ]]; then
            configure_export ccache "$CCACHE_SEED_REF" trtllm-ccache-output-base ccache
          fi
          if [[ -n "$WHEEL_TAG" ]]; then
            configure_export wheel "$WHEEL_SEED_REF" trtllm-wheel-cache-output-base "wheel cache"
          fi

      - name: Get recently merged PR cache refs
        id: recent_pr_cache_refs
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        env:
          REGISTRY: ${{ inputs.registry }}
          IMAGE_NAME: ${{ inputs.image-name }}
        with:
          script: |
            const [owner, repo] = process.env.GITHUB_REPOSITORY.split("/");
            const result = await github.graphql(`
              query($owner: String!, $repo: String!) {
                repository(owner: $owner, name: $repo) {
                  pullRequests(states: MERGED, first: 100, orderBy: {field: UPDATED_AT, direction: DESC}) {
                    nodes {
                      number
                    }
                  }
                }
              }
            `, { owner, repo });

            const refs = result.repository.pullRequests.nodes
              .map(({ number }) => `type=registry,ref=${process.env.REGISTRY}/${process.env.IMAGE_NAME}:${number}-buildcache,mode=max`)
              .join("\n");

            core.setOutput("cache-from", refs);
            core.info(`Found ${result.repository.pullRequests.nodes.length} recently merged PR cache refs.`);

      - name: Compute build metadata
        id: build_meta
        shell: bash
        run: |
          set -euo pipefail

          PR_NUMBER=""
          if [[ "$GH_REF" =~ refs/heads/pull-request/([0-9]+) ]]; then
            PR_NUMBER="${BASH_REMATCH[1]}"
          fi

          TAGS=("$REGISTRY/$IMAGE_NAME:$RUN_ID")
          if [[ "$GH_REF" == "refs/heads/main" ]]; then
            CACHE_KEY="main"
            TAGS+=("$REGISTRY/$IMAGE_NAME:main")
          elif [[ -n "$PR_NUMBER" ]]; then
            CACHE_KEY="$PR_NUMBER"
            TAGS+=("$REGISTRY/$IMAGE_NAME:$PR_NUMBER")
          else
            CACHE_KEY=$(printf '%s' "${GITHUB_REF_NAME:-$RUN_ID}" | tr '/' '-' | tr -cd '[:alnum:]._-')
            if [[ -z "$CACHE_KEY" ]]; then
              CACHE_KEY="$RUN_ID"
            fi
          fi

          CACHE_FROM=(
            "type=registry,ref=$REGISTRY/$IMAGE_NAME:main-buildcache,mode=max"
          )
          if [[ "$CACHE_KEY" != "main" ]]; then
            CACHE_FROM+=("type=registry,ref=$REGISTRY/$IMAGE_NAME:$CACHE_KEY-buildcache,mode=max")
          fi

          {
            echo "tags<<EOF"
            printf '%s\n' "${TAGS[@]}"
            echo "EOF"
            echo "cache-from<<EOF"
            printf '%s\n' "${CACHE_FROM[@]}"
            echo "EOF"
            echo "cache-to=type=registry,ref=$REGISTRY/$IMAGE_NAME:$CACHE_KEY-buildcache,mode=max"
          } >> "$GITHUB_OUTPUT"

      - name: Build and push
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5
        with:
          file: ${{ inputs.dockerfile }}
          push: true
          context: .
          platforms: ${{ inputs.platform }}
          build-contexts: |
            ${{ inputs.build-contexts }}
            ${{ steps.trtllm_cache_seed.outputs.ccache_context }}
            ${{ steps.trtllm_cache_seed.outputs.wheel_context }}
          build-args: ${{ inputs.build-args }}
          cache-from: |
            ${{ steps.build_meta.outputs.cache-from }}
            ${{ steps.recent_pr_cache_refs.outputs.cache-from }}
          cache-to: ${{ steps.build_meta.outputs.cache-to }}
          no-cache: false
          tags: |
            ${{ steps.build_meta.outputs.tags }}
          target: ${{ inputs.target }}

      - name: Probe TRT-LLM incremental cache exports
        id: trtllm_cache_export_status
        if: ${{ always() && !cancelled() && steps.trtllm_cache.outputs.enabled == 'true' && steps.trtllm_cache_export.outcome == 'success' && (steps.trtllm_cache_export.outputs.ccache_mode == 'incremental' || steps.trtllm_cache_export.outputs.wheel_mode == 'incremental') }}
        # This is an optional churn optimization. If the probe fails, leave its
        # outputs unset so the cache update steps below retain their old,
        # fail-open behavior and push.
        continue-on-error: true
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5
        with:
          file: ${{ inputs.dockerfile }}
          push: false
          context: .
          platforms: ${{ inputs.platform }}
          build-contexts: |
            ${{ inputs.build-contexts }}
            ${{ steps.trtllm_cache_seed.outputs.ccache_context }}
            ${{ steps.trtllm_cache_seed.outputs.wheel_context }}
          build-args: |
            ${{ inputs.build-args }}
            CCACHE_EXPORT_NONCE=${{ github.run_id }}-${{ github.run_attempt }}
            CCACHE_EXPORT_MODE=${{ steps.trtllm_cache_export.outputs.ccache_mode || 'full' }}
            WHEEL_CACHE_EXPORT_NONCE=${{ github.run_id }}-${{ github.run_attempt }}
            WHEEL_CACHE_EXPORT_MODE=${{ steps.trtllm_cache_export.outputs.wheel_mode || 'full' }}
          cache-from: |
            ${{ steps.build_meta.outputs.cache-from }}
            ${{ steps.recent_pr_cache_refs.outputs.cache-from }}
          no-cache: false
          provenance: false
          outputs: type=local,dest=${{ runner.temp }}/trtllm-cache-export-status-${{ github.run_id }}-${{ github.run_attempt }}
          target: trtllm-cache-export-status

      - name: Check TRT-LLM incremental cache deltas
        id: trtllm_cache_delta
        if: steps.trtllm_cache_export_status.outcome == 'success'
        continue-on-error: true
        shell: bash
        env:
          STATUS_DIR: ${{ runner.temp }}/trtllm-cache-export-status-${{ github.run_id }}-${{ github.run_attempt }}
          CCACHE_MODE: ${{ steps.trtllm_cache_export.outputs.ccache_mode }}
          WHEEL_MODE: ${{ steps.trtllm_cache_export.outputs.wheel_mode }}
        run: |
          set -euo pipefail

          resolve_delta() {
            local mode="$1"
            local status_file="$2"
            if [[ "$mode" != "incremental" ]]; then
              echo true
              return
            fi
            [[ -f "$status_file" ]] || return 1
            local has_delta
            has_delta=$(<"$status_file")
            [[ "$has_delta" == "true" || "$has_delta" == "false" ]] || return 1
            echo "$has_delta"
          }

          CCACHE_HAS_DELTA=$(resolve_delta "$CCACHE_MODE" "$STATUS_DIR/ccache-has-delta")
          WHEEL_HAS_DELTA=$(resolve_delta "$WHEEL_MODE" "$STATUS_DIR/wheel-has-delta")
          {
            echo "ccache_has_delta=$CCACHE_HAS_DELTA"
            echo "wheel_has_delta=$WHEEL_HAS_DELTA"
          } >> "$GITHUB_OUTPUT"
          echo "TRT-LLM incremental cache deltas: ccache=$CCACHE_HAS_DELTA, wheel=$WHEEL_HAS_DELTA"

      - name: Update TRT-LLM ccache image
        # Cache mounts survive a failed RUN in the current Buildx builder. Run
        # this step after both successful and failed image builds so completed
        # compiler outputs are preserved for the next retry.
        if: ${{ always() && !cancelled() && steps.trtllm_cache.outputs.enabled == 'true' && steps.trtllm_cache.outputs.ccache_tag != '' && steps.trtllm_cache_export.outcome == 'success' && steps.trtllm_cache_delta.outputs.ccache_has_delta != 'false' }}
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5
        with:
          file: ${{ inputs.dockerfile }}
          push: true
          context: .
          platforms: ${{ inputs.platform }}
          build-contexts: |
            ${{ inputs.build-contexts }}
            ${{ steps.trtllm_cache_seed.outputs.ccache_context }}
            ${{ steps.trtllm_cache_export.outputs.ccache_output_context }}
          build-args: |
            ${{ inputs.build-args }}
            CCACHE_EXPORT_NONCE=${{ github.run_id }}-${{ github.run_attempt }}
            CCACHE_EXPORT_MODE=${{ steps.trtllm_cache_export.outputs.ccache_mode }}
          cache-from: |
            ${{ steps.build_meta.outputs.cache-from }}
            ${{ steps.recent_pr_cache_refs.outputs.cache-from }}
          no-cache: false
          provenance: false
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.trtllm_cache.outputs.ccache_tag }}
          target: trtllm-ccache

      - name: Update TRT-LLM wheel cache image
        # Persist the custom wheel even when the release build fails, so a
        # retry can reuse completed compilation work.
        if: ${{ always() && !cancelled() && steps.trtllm_cache.outputs.enabled == 'true' && steps.trtllm_cache.outputs.wheel_tag != '' && steps.trtllm_cache_export.outcome == 'success' && steps.trtllm_cache_delta.outputs.wheel_has_delta != 'false' }}
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5
        with:
          file: ${{ inputs.dockerfile }}
          push: true
          context: .
          platforms: ${{ inputs.platform }}
          build-contexts: |
            ${{ inputs.build-contexts }}
            ${{ steps.trtllm_cache_seed.outputs.wheel_context }}
            ${{ steps.trtllm_cache_export.outputs.wheel_output_context }}
          build-args: |
            ${{ inputs.build-args }}
            WHEEL_CACHE_EXPORT_NONCE=${{ github.run_id }}-${{ github.run_attempt }}
            WHEEL_CACHE_EXPORT_MODE=${{ steps.trtllm_cache_export.outputs.wheel_mode }}
          cache-from: |
            ${{ steps.build_meta.outputs.cache-from }}
            ${{ steps.recent_pr_cache_refs.outputs.cache-from }}
          no-cache: false
          provenance: false
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.trtllm_cache.outputs.wheel_tag }}
          target: trtllm-wheel-cache
